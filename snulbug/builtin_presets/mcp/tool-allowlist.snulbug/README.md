# MCP Tool Allowlist

Minimal MCP policy that rejects unlisted `tools/call` names and allows
non-tool JSON-RPC methods to pass through.

Edit `policy.lua` after copying the preset to change the allowed tools.
